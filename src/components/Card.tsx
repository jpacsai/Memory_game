import React from "react";
import './Card.scss';

class Card extends React.PureComponent {
  render() {
    return (
      <div className="Card">
        'Card'
      </div>
    );
  }
}

export default Card;
